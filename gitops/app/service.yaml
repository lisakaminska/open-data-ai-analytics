apiVersion: v1
kind: Service
metadata:
  name: mvc-web-service
  namespace: mvc-analytics
spec:
  selector:
    app: mvc-web
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30500
  type: NodePort
